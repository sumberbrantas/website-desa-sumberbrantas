"use client";

import { useEffect } from "react";
import { app, analytics } from "@/lib/firebase";

const FirebaseInit = () => {
  useEffect(() => {
  }, []);

  return null;
};

export default FirebaseInit;

